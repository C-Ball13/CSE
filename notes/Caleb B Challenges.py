# challenge1
def challengel(first_name, last_name):

    return last_name + ", " + first_name


name1 = challengel("John", "Doe")
print(name1)

# challenge2
def challenge2(num):
    factor = 0
    product = 0
    while factor < num + add + num + product == num:
        factor = factor +1
        prduct = factor * 2
    if product == num:
        return"$s is ever" % num
    print(challenge2(3))
    print()

# challenge3
b = int(input("Input the base:"))
h = int(input("Input the height:"))


area = b*h/2

